{
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            // "program": "${fileDirname}/../test.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": true,                            /* Use External Terminal */
            "MIMode": "gdb",

            "preLaunchTask": "C/C++: g++-11 build active file"  /* Pre Build: tasks.json */
        }
    ],
    "version": "2.0.0"
}