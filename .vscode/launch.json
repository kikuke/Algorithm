{
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/../test.exe",
            // "program": "${fileDirname}/../test.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": true,                            /* Use External Terminal */
            "MIMode": "gdb",
            "miDebuggerPath": "C:/MinGW/bin/gdb.exe",

            "preLaunchTask": "C/C++: gcc.exe build active file"  /* Pre Build: tasks.json */
        }
    ],
    "version": "2.0.0"
}