#include <stdio.h>

int N;  /* 초밥 접시 수 N */
int D;  /* 초밥 가짓 수 D */
int K;  /* 연속 먹는 점시 개수 K */
int C;  /* 쿠폰 번호 */
int belt[30000];

int main(void)
{
    scanf("%d %d %d %d", &N, &D, &K, &C);
    for (int i = 0; i < N; i++) {
        int sushi;
        scanf("%d", &sushi);
    }

    /* K개 할당량 맞춰야함 */
    return 0;
}